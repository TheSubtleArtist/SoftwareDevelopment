#include <stdio.h>


int main() {
  	char name[] = "DannyBoy";
    int age = 934;
  	printf("Hello, %s, welcome to the c-matrix", name);
  	printf("I expect to become a C gury by %d years old.", age);
    return(0);
}